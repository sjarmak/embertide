# cathedral_altar_frame_region

- **Bead:** embertide-u10c
- **Card:** N/A — altar-frame overlay (shared, region variant)
- **Model:** fal-ai/nano-banana-pro
- **Params:** `aspect_ratio=3:4`, `output_format=png`, `resolution=1K`, `num_images=1`
- **Raster:** `public/illustrations/cathedral_altar_frame_region_001.webp`
- **Spec:** `src/illustration/examples/cathedral_altar_frame_region.json`

## Prompt

Transparent PNG cut-out illustration, portrait 3:4 aspect ratio, in the Elysian Cathedral card-art style: an ornate carved stone altar archway frame, rendered as a flat decorative overlay with a LARGE TRANSPARENT OPENING in the center so a background shows through — the frame itself occupies only the outer ~25% perimeter of the image. Arch frame composition: dark obsidian-black cathedral stone veined with deep crimson and iron gunmetal, heavier and more ominous than an ordinary altar, twin fluted pillars on left and right banded with ruby-red ironwork flourishes and half-strangled by thorned blackened ivy vines, an Embertide motif (an off-center whorl of molten crimson-gold ember-light coiling out of an ethereal field, like a captured aurora drawn into a spiral) carved in relief at the apex keystone glowing faint ember-red, trefoil quatrefoil cornices in the arch spandrels inlaid with ruby cabochons, and two LIT iron torch sconces mounted at the base of each pillar with warm orange flames licking upward casting glow on the lower stone (flames baked into the raster — the CSS ember pulse animates separately). Thick dark leading lines outline every stone block and thorn. Dramatic directional cathedral light from the upper left with heavy shadow in the crevices. The ENTIRE CENTER and inside of the arch MUST be fully transparent (alpha 0) — no glass, no sky, no fill, just the cutout so a stained-glass pane behind can show through. No text, no letters, no watermarks, no UI overlays, no background — just the frame on a transparent canvas.
